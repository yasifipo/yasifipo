# v0.01a
  * PREZ: list of prez
  * TAG: list of tagtypes
  * ALL: check TODO label in code :)
  * COLLECTION: README to update
	* POST: README to update
	* POST : label for next / prev --> in i18n file
	* POST: Post series
	* ALL: basic template + css
  * ALL: example _data : to be completed:
    *  each type of data
    *  templates
	* ADMIN: login required
	* ALL: unicode stuff:
	  *  at new lang generation
		*  on windows --> add , encoding="utf-8" for any with open(*)
  * INSTALL: download reveal.js
  * favicon

# v0.01b
  * ALL: check all yaml, that mandatory are present / format (exple: sort is INT)
  * ALL: ALL: verify that a slug/rule is not used already



# next
  * ALL: yaml editor
  * ALL: category parents
  * PAGE: children for pages (to be able to construct ToC)
  * CATEGORY:  default cat if not provided --> No, but a way to retrieve all not cat pages/prez/toc/single/blogpost (by plugin)?
	* PLUGIN management
  * ALL: see TARGET file
